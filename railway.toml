[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthCheckPath = "/health"
healthCheckTimeout = 100

[services.ride-for-you]
# Unified service configuration
